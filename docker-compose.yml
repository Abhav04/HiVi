version: "3.9"

services:

  postgres:
    image: postgres:15
    container_name: postgres
    environment:
      POSTGRES_DB: editorplatform
      POSTGRES_USER: abhavkushwaha
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"
  mailhog:
    image: mailhog/mailhog
    container_name: mailhog
    ports:
      - "1025:1025" #SMTP (Spring Boot connects here)

      - "8025:8025" #Web UI (you see emails here)
